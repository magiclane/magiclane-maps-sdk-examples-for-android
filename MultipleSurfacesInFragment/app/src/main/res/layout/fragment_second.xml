<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".SecondFragment">

        <ScrollView
            android:id="@+id/scroll_view"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:fadeScrollbars="false"
            android:scrollbars="vertical"
            android:fillViewport="true"
            app:layout_constraintBottom_toTopOf="@id/bottom_buttons"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:paddingLeftWithSystemWindowInsets="@{0f}"
            app:paddingRightWithSystemWindowInsets="@{0f}"
            >

            <LinearLayout
                android:id="@+id/scrolled_linear_layout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

            </LinearLayout>

        </ScrollView>

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/bottom_buttons"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/mapButtonMargin"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:orientation="horizontal"
            android:paddingTop="@dimen/small_padding"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:marginLeftWithSystemWindowInsets="@{0f}"
            app:marginRightWithSystemWindowInsets="@{0f}">

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/add_surface_button"
                android:layout_width="@dimen/bottom_action_fab_size"
                android:layout_height="@dimen/bottom_action_fab_size"
                android:visibility="invisible"
                app:backgroundTint="@color/primary"
                app:borderWidth="0dp"
                app:elevation="0dp"
                app:pressedTranslationZ="0dp"
                app:fabCustomSize="@dimen/bottom_action_fab_size"
                app:maxImageSize="@dimen/bottom_action_fab_icon_size"
                app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlayCircleFab"
                app:srcCompat="@drawable/ic_minus_symbol"
                app:tint="@color/on_primary"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:ignore="ContentDescription"
                tools:visibility="visible"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/previous_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/previous"
                app:layout_constraintStart_toEndOf="@id/add_surface_button"
                app:layout_constraintTop_toTopOf="@id/add_surface_button"
                app:layout_constraintBottom_toBottomOf="@id/add_surface_button"
                app:layout_constraintEnd_toStartOf="@id/remove_surface_button" />

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/remove_surface_button"
                android:layout_width="@dimen/bottom_action_fab_size"
                android:layout_height="@dimen/bottom_action_fab_size"
                android:visibility="invisible"
                app:backgroundTint="@color/primary"
                app:borderWidth="0dp"
                app:elevation="0dp"
                app:pressedTranslationZ="0dp"
                app:fabCustomSize="@dimen/bottom_action_fab_size"
                app:maxImageSize="@dimen/bottom_action_fab_icon_size"
                app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlayCircleFab"
                app:srcCompat="@drawable/ic_plus_symbol"
                app:tint="@color/on_primary"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:ignore="ContentDescription"
                tools:visibility="visible"/>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
