<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	android:background="?android:windowBackground"
	android:orientation="vertical">

	<androidx.appcompat.widget.Toolbar
		android:id="@+id/toolbar"
		android:layout_width="match_parent"
		android:layout_height="?attr/actionBarSize"
		android:background="?attr/colorPrimary"
		app:layout_constraintBottom_toTopOf="@id/search_address_form"
		app:layout_constraintTop_toTopOf="parent"
		app:popupTheme="@style/AppTheme.PopupOverlay"
		app:titleTextColor="@android:color/white"></androidx.appcompat.widget.Toolbar>

	<ProgressBar
		android:id="@+id/progressBar"
		style="?android:attr/progressBarStyle"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		app:layout_constraintBottom_toBottomOf="parent"
		app:layout_constraintEnd_toEndOf="parent"
		app:layout_constraintStart_toStartOf="parent"
		app:layout_constraintTop_toTopOf="parent" />

	<RelativeLayout
		android:id="@+id/search_address_form"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		app:layout_constraintBottom_toTopOf="@id/header_divider"
		app:layout_constraintTop_toBottomOf="@id/toolbar">

		<RelativeLayout
			android:id="@+id/city_street_container"
			android:layout_width="match_parent"
			android:layout_height="wrap_content">

			<LinearLayout
				android:id="@+id/flag_city_container"
				android:layout_width="match_parent"
				android:layout_height="wrap_content"
				android:orientation="horizontal">

				<ImageView
					android:id="@+id/flag_icon"
					android:layout_width="@dimen/listIconSize"
					android:layout_height="@dimen/listIconSize"
					android:layout_gravity="center_vertical"
					android:layout_marginLeft="@dimen/smallPadding"
					android:layout_marginRight="@dimen/smallPadding"
					android:scaleType="fitCenter"
					tools:ignore="ContentDescription" />

				<EditText
					android:id="@+id/state"
					android:layout_width="wrap_content"
					android:layout_height="wrap_content"
					android:layout_gravity="center_vertical"
					android:layout_marginEnd="@dimen/smallPadding"
					android:backgroundTint="@color/colorPrimary"
					android:importantForAutofill="no"
					android:inputType="text"
					tools:ignore="LabelFor" />

				<androidx.appcompat.widget.AppCompatEditText
					android:id="@+id/city"
					android:layout_width="0dip"
					android:layout_height="wrap_content"
					android:layout_gravity="center_vertical"
					android:layout_marginEnd="@dimen/smallPadding"
					android:layout_weight="2"
					android:backgroundTint="@color/colorPrimary" />
				<!-- style="@style/AddressSearchEditText" can be changed to textNoSuggestions from API level 5  -->

			</LinearLayout>

			<androidx.appcompat.widget.AppCompatEditText
				android:id="@+id/street"
				android:layout_width="match_parent"
				android:layout_height="wrap_content"
				android:layout_below="@id/flag_city_container"
				android:layout_alignParentStart="true"
				android:layout_alignParentEnd="true"
				android:layout_marginLeft="@dimen/smallPadding"
				android:layout_marginRight="@dimen/smallPadding"
				android:backgroundTint="@color/colorPrimary" />

		</RelativeLayout>

		<LinearLayout
			android:id="@+id/submit_container"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:layout_below="@id/city_street_container"
			android:orientation="horizontal">

			<androidx.appcompat.widget.AppCompatEditText
				android:id="@+id/street_number"
				android:layout_width="@dimen/address_search_number_width"
				android:layout_height="wrap_content"
				android:layout_gravity="center_vertical"
				android:layout_marginLeft="@dimen/smallPadding"
				android:layout_marginRight="@dimen/smallPadding"
				android:backgroundTint="@color/colorPrimary"
				android:imeOptions="actionSearch" />

			<androidx.appcompat.widget.AppCompatEditText
				android:id="@+id/intersection"
				android:layout_width="0dip"
				android:layout_height="wrap_content"
				android:layout_gravity="center_vertical"
				android:layout_weight="2"
				android:backgroundTint="@color/colorPrimary"
				android:imeOptions="actionSearch" />

			<ImageView
				android:id="@+id/submit_button"
				android:layout_width="@dimen/listIconSize"
				android:layout_height="@dimen/listIconSize"
				android:layout_gravity="center_vertical"
				android:layout_marginEnd="@dimen/smallPadding"
				android:adjustViewBounds="true"
				android:cropToPadding="false"
				tools:ignore="ContentDescription" />

		</LinearLayout>

	</RelativeLayout>

	<LinearLayout
		android:id="@+id/header_divider"
		android:layout_width="match_parent"
		android:layout_height="1dip"
		android:background="?android:attr/colorForeground"
		android:orientation="vertical"
		app:layout_constraintBottom_toTopOf="@id/list1"
		app:layout_constraintTop_toBottomOf="@id/search_address_form" />

	<RelativeLayout
		android:id="@+id/list1"
		android:layout_width="match_parent"
		android:layout_height="0dp"
		app:layout_constraintBottom_toBottomOf="parent"
		app:layout_constraintTop_toBottomOf="@id/header_divider">

		<ListView
			android:id="@+id/recent_list"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:cacheColorHint="@android:color/transparent" />

		<View
			android:id="@+id/generic_activity_top_container_shadow"
			android:layout_width="match_parent"
			android:layout_height="3dip"
			android:layout_alignParentTop="true"
			android:background="@drawable/decor_shadow_bottom"
			android:visibility="gone" />

	</RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
