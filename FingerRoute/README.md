## Overview

This example app demonstrates the following features:
- Calculates a route that tries to follow finger movement on the map
- Route type can be selected from context menu (either bike or pedestrian)
- After route is computed, finger line visibility on the map can be toggled via top right button tap
- The resulting route can be shared as GPX file
- A custom style is loaded to be able to render route on top of finger line

## Requirements

- Deployment target device must run Android 5.0 (API level 21) or higher.
- Deployment target device must be connected to internet.

## Build instructions

Step 1. Download the SDK

Step 2. Extract SDK to the predefined folder (app/libs)

Step 3. Load the project into ```Android Studio```.

Step 4. ```File``` -> ```Sync Project with Gradle Files```.

Step 5. Select desired ```Active Build Variant``` (debug/release) in ```Build Variants``` menu.

Step 6. Deploy to device as usual.
